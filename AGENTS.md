# Animus

Python 3.11+ personality calculator. No servers or databases.

## Setup

```bash
python3 -m pip install -e '.[dev]'
```

## Tests

```bash
python3 -m pytest
```

Use `python3 -m pytest` rather than the `pytest` script; pip may install console scripts under `~/.local/bin`.
